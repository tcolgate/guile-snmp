/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "gstTabATable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement gstTabATable get routines.
 * TODO:240:M: Implement gstTabATable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table gstTabATable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * GUILE-SNMP-TEST-MIB::gstTabATable is subid 3 of gstMibObjects.
 * Its status is Current.
 * OID: .1.3.6.1.3.1977.1.3, length: 8
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement gstTabATable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param gstTabAIndex_val
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
gstTabATable_indexes_set_tbl_idx(gstTabATable_mib_index *tbl_idx, long gstTabAIndex_val)
{
    DEBUGMSGTL(("verbose:gstTabATable:gstTabATable_indexes_set_tbl_idx","called\n"));

    /* gstTabAIndex(1)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/R/d/h */
    tbl_idx->gstTabAIndex = gstTabAIndex_val;
    

    return MFD_SUCCESS;
} /* gstTabATable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
gstTabATable_indexes_set(gstTabATable_rowreq_ctx *rowreq_ctx, long gstTabAIndex_val)
{
    DEBUGMSGTL(("verbose:gstTabATable:gstTabATable_indexes_set","called\n"));

    if(MFD_SUCCESS != gstTabATable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , gstTabAIndex_val
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != gstTabATable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* gstTabATable_indexes_set */


/*---------------------------------------------------------------------
 * GUILE-SNMP-TEST-MIB::gstTabAEntry.gstTabAData
 * gstTabAData is subid 2 of gstTabAEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.3.1977.1.3.1.2
 * Description:
Test tableA entry data
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the gstTabAData data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param gstTabAData_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param gstTabAData_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by gstTabAData.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*gstTabAData_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update gstTabAData_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
gstTabAData_get( gstTabATable_rowreq_ctx *rowreq_ctx, char **gstTabAData_val_ptr_ptr, size_t *gstTabAData_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != gstTabAData_val_ptr_ptr) && (NULL != *gstTabAData_val_ptr_ptr));
   netsnmp_assert( NULL != gstTabAData_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:gstTabATable:gstTabAData_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the gstTabAData data.
 * copy (* gstTabAData_val_ptr_ptr ) data and (* gstTabAData_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for gstTabAData data
     */
    if ((NULL == (* gstTabAData_val_ptr_ptr )) ||
        ((* gstTabAData_val_ptr_len_ptr ) <
         (rowreq_ctx->data.gstTabAData_len* sizeof(rowreq_ctx->data.gstTabAData[0])))) {
        /*
         * allocate space for gstTabAData data
         */
        (* gstTabAData_val_ptr_ptr ) = malloc(rowreq_ctx->data.gstTabAData_len* sizeof(rowreq_ctx->data.gstTabAData[0]));
        if(NULL == (* gstTabAData_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* gstTabAData_val_ptr_len_ptr ) = rowreq_ctx->data.gstTabAData_len* sizeof(rowreq_ctx->data.gstTabAData[0]);
    memcpy( (* gstTabAData_val_ptr_ptr ), rowreq_ctx->data.gstTabAData, rowreq_ctx->data.gstTabAData_len* sizeof(rowreq_ctx->data.gstTabAData[0]) );

    return MFD_SUCCESS;
} /* gstTabAData_get */



/** @} */
