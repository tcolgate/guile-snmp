/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 12088 $ of $ 
 *
 * $Id:$
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "gstTabBTable.h"


/** @defgroup data_get data_get: Routines to get data
 *
 * TODO:230:M: Implement gstTabBTable get routines.
 * TODO:240:M: Implement gstTabBTable mapping routines (if any).
 *
 * These routine are used to get the value for individual objects. The
 * row context is passed, along with a pointer to the memory where the
 * value should be copied.
 *
 * @{
 */
/**********************************************************************
 **********************************************************************
 ***
 *** Table gstTabBTable
 ***
 **********************************************************************
 **********************************************************************/
/*
 * GUILE-SNMP-TEST-MIB::gstTabBTable is subid 4 of gstMibObjects.
 * Its status is Current.
 * OID: .1.3.6.1.3.1977.1.4, length: 8
*/

/* ---------------------------------------------------------------------
 * TODO:200:r: Implement gstTabBTable data context functions.
 */


/**
 * set mib index(es)
 *
 * @param tbl_idx mib index structure
 * @param gstTabBIndex_val
 * @param gstTabBIndex2_ptr
 * @param gstTabBIndex2_ptr_len
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This convenience function is useful for setting all the MIB index
 *  components with a single function call. It is assume that the C values
 *  have already been mapped from their native/rawformat to the MIB format.
 */
int
gstTabBTable_indexes_set_tbl_idx(gstTabBTable_mib_index *tbl_idx, long gstTabBIndex_val, char *gstTabBIndex2_val_ptr,  size_t gstTabBIndex2_val_ptr_len)
{
    DEBUGMSGTL(("verbose:gstTabBTable:gstTabBTable_indexes_set_tbl_idx","called\n"));

    /* gstTabBIndex(1)/INTEGER32/ASN_INTEGER/long(long)//l/A/w/e/R/d/h */
    tbl_idx->gstTabBIndex = gstTabBIndex_val;
    
    /* gstTabBIndex2(2)/DisplayString/ASN_OCTET_STR/char(char)//L/A/w/e/R/d/H */
    tbl_idx->gstTabBIndex2_len = sizeof(tbl_idx->gstTabBIndex2)/sizeof(tbl_idx->gstTabBIndex2[0]); /* max length */
    /*
     * make sure there is enough space for gstTabBIndex2 data
     */
    if ((NULL == tbl_idx->gstTabBIndex2) ||
        (tbl_idx->gstTabBIndex2_len <
         (gstTabBIndex2_val_ptr_len))) {
        snmp_log(LOG_ERR,"not enough space for value\n");
        return MFD_ERROR;
    }
    tbl_idx->gstTabBIndex2_len = gstTabBIndex2_val_ptr_len;
    memcpy( tbl_idx->gstTabBIndex2, gstTabBIndex2_val_ptr, gstTabBIndex2_val_ptr_len* sizeof(gstTabBIndex2_val_ptr[0]) );
    

    return MFD_SUCCESS;
} /* gstTabBTable_indexes_set_tbl_idx */

/**
 * @internal
 * set row context indexes
 *
 * @param reqreq_ctx the row context that needs updated indexes
 *
 * @retval MFD_SUCCESS     : success.
 * @retval MFD_ERROR       : other error.
 *
 * @remark
 *  This function sets the mib indexs, then updates the oid indexs
 *  from the mib index.
 */
int
gstTabBTable_indexes_set(gstTabBTable_rowreq_ctx *rowreq_ctx, long gstTabBIndex_val, char *gstTabBIndex2_val_ptr,  size_t gstTabBIndex2_val_ptr_len)
{
    DEBUGMSGTL(("verbose:gstTabBTable:gstTabBTable_indexes_set","called\n"));

    if(MFD_SUCCESS != gstTabBTable_indexes_set_tbl_idx(&rowreq_ctx->tbl_idx
                                   , gstTabBIndex_val
                                   , gstTabBIndex2_val_ptr, gstTabBIndex2_val_ptr_len
           ))
        return MFD_ERROR;

    /*
     * convert mib index to oid index
     */
    rowreq_ctx->oid_idx.len = sizeof(rowreq_ctx->oid_tmp) / sizeof(oid);
    if(0 != gstTabBTable_index_to_oid(&rowreq_ctx->oid_idx,
                                    &rowreq_ctx->tbl_idx)) {
        return MFD_ERROR;
    }

    return MFD_SUCCESS;
} /* gstTabBTable_indexes_set */


/*---------------------------------------------------------------------
 * GUILE-SNMP-TEST-MIB::gstTabBEntry.gstTabBData
 * gstTabBData is subid 3 of gstTabBEntry.
 * Its status is Current, and its access level is ReadOnly.
 * OID: .1.3.6.1.3.1977.1.4.1.3
 * Description:
Test tableB entry data
 *
 * Attributes:
 *   accessible 1     isscalar 0     enums  0      hasdefval 0
 *   readable   1     iscolumn 1     ranges 1      hashint   1
 *   settable   0
 *   hint: 255a
 *
 * Ranges:  0 - 255;
 *
 * Its syntax is DisplayString (based on perltype OCTETSTR)
 * The net-snmp type is ASN_OCTET_STR. The C type decl is char (char)
 * This data type requires a length.  (Max 255)
 */
/**
 * Extract the current value of the gstTabBData data.
 *
 * Set a value using the data context for the row.
 *
 * @param rowreq_ctx
 *        Pointer to the row request context.
 * @param gstTabBData_val_ptr_ptr
 *        Pointer to storage for a char variable
 * @param gstTabBData_val_ptr_len_ptr
 *        Pointer to a size_t. On entry, it will contain the size (in bytes)
 *        pointed to by gstTabBData.
 *        On exit, this value should contain the data size (in bytes).
 *
 * @retval MFD_SUCCESS         : success
 * @retval MFD_SKIP            : skip this node (no value for now)
 * @retval MFD_ERROR           : Any other error
*
 * @note If you need more than (*gstTabBData_val_ptr_len_ptr) bytes of memory,
 *       allocate it using malloc() and update gstTabBData_val_ptr_ptr.
 *       <b>DO NOT</b> free the previous pointer.
 *       The MFD helper will release the memory you allocate.
 *
 * @remark If you call this function yourself, you are responsible
 *         for checking if the pointer changed, and freeing any
 *         previously allocated memory. (Not necessary if you pass
 *         in a pointer to static memory, obviously.)
 */
int
gstTabBData_get( gstTabBTable_rowreq_ctx *rowreq_ctx, char **gstTabBData_val_ptr_ptr, size_t *gstTabBData_val_ptr_len_ptr )
{
   /** we should have a non-NULL pointer and enough storage */
   netsnmp_assert( (NULL != gstTabBData_val_ptr_ptr) && (NULL != *gstTabBData_val_ptr_ptr));
   netsnmp_assert( NULL != gstTabBData_val_ptr_len_ptr );


    DEBUGMSGTL(("verbose:gstTabBTable:gstTabBData_get","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);

/*
 * TODO:231:o: |-> Extract the current value of the gstTabBData data.
 * copy (* gstTabBData_val_ptr_ptr ) data and (* gstTabBData_val_ptr_len_ptr ) from rowreq_ctx->data
 */
    /*
     * make sure there is enough space for gstTabBData data
     */
    if ((NULL == (* gstTabBData_val_ptr_ptr )) ||
        ((* gstTabBData_val_ptr_len_ptr ) <
         (rowreq_ctx->data.gstTabBData_len* sizeof(rowreq_ctx->data.gstTabBData[0])))) {
        /*
         * allocate space for gstTabBData data
         */
        (* gstTabBData_val_ptr_ptr ) = malloc(rowreq_ctx->data.gstTabBData_len* sizeof(rowreq_ctx->data.gstTabBData[0]));
        if(NULL == (* gstTabBData_val_ptr_ptr )) {
            snmp_log(LOG_ERR,"could not allocate memory\n");
            return MFD_ERROR;
        }
    }
    (* gstTabBData_val_ptr_len_ptr ) = rowreq_ctx->data.gstTabBData_len* sizeof(rowreq_ctx->data.gstTabBData[0]);
    memcpy( (* gstTabBData_val_ptr_ptr ), rowreq_ctx->data.gstTabBData, rowreq_ctx->data.gstTabBData_len* sizeof(rowreq_ctx->data.gstTabBData[0]) );

    return MFD_SUCCESS;
} /* gstTabBData_get */



/** @} */
