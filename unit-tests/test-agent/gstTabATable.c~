/*
 * Note: this file originally auto-generated by mib2c using
 *       version : 14170 $ of $ 
 *
 * $Id:$
 */
/** \page MFD helper for gstTabATable
 *
 * \section intro Introduction
 * Introductory text.
 *
 */
/* standard Net-SNMP includes */
#include <net-snmp/net-snmp-config.h>
#include <net-snmp/net-snmp-includes.h>
#include <net-snmp/agent/net-snmp-agent-includes.h>

/* include our parent header */
#include "gstTabATable.h"

#include <net-snmp/agent/mib_modules.h>

#include "gstTabATable_interface.h"

oid gstTabATable_oid[] = { GSTTABATABLE_OID };
int gstTabATable_oid_size = OID_LENGTH(gstTabATable_oid);

    gstTabATable_registration  gstTabATable_user_context;

void initialize_table_gstTabATable(void);
void shutdown_table_gstTabATable(void);


/**
 * Initializes the gstTabATable module
 */
void
init_gstTabATable(void)
{
    DEBUGMSGTL(("verbose:gstTabATable:init_gstTabATable","called\n"));

    /*
     * TODO:300:o: Perform gstTabATable one-time module initialization.
     */
     
    /*
     * here we initialize all the tables we're planning on supporting
     */
    if (should_init("gstTabATable"))
        initialize_table_gstTabATable();

} /* init_gstTabATable */

/**
 * Shut-down the gstTabATable module (agent is exiting)
 */
void
shutdown_gstTabATable(void)
{
    if (should_init("gstTabATable"))
        shutdown_table_gstTabATable();

}

/**
 * Initialize the table gstTabATable 
 *    (Define its contents and how it's structured)
 */
void
initialize_table_gstTabATable(void)
{
    gstTabATable_registration * user_context;
    u_long flags;

    DEBUGMSGTL(("verbose:gstTabATable:initialize_table_gstTabATable","called\n"));

    /*
     * TODO:301:o: Perform gstTabATable one-time table initialization.
     */

    /*
     * TODO:302:o: |->Initialize gstTabATable user context
     * if you'd like to pass in a pointer to some data for this
     * table, allocate or set it up here.
     */
    /*
     * a netsnmp_data_list is a simple way to store void pointers. A simple
     * string token is used to add, find or remove pointers.
     */
    user_context = netsnmp_create_data_list("gstTabATable", NULL, NULL);
    
    /*
     * No support for any flags yet, but in the future you would
     * set any flags here.
     */
    flags = 0;
    
    /*
     * call interface initialization code
     */
    _gstTabATable_initialize_interface(user_context, flags);
} /* initialize_table_gstTabATable */

/**
 * Shutdown the table gstTabATable 
 */
void
shutdown_table_gstTabATable(void)
{
    /*
     * call interface shutdown code
     */
    _gstTabATable_shutdown_interface(&gstTabATable_user_context);
}

/**
 * extra context initialization (eg default values)
 *
 * @param rowreq_ctx    : row request context
 * @param user_init_ctx : void pointer for user (parameter to rowreq_ctx_allocate)
 *
 * @retval MFD_SUCCESS  : no errors
 * @retval MFD_ERROR    : error (context allocate will fail)
 */
int
gstTabATable_rowreq_ctx_init(gstTabATable_rowreq_ctx *rowreq_ctx,
                           void *user_init_ctx)
{
    DEBUGMSGTL(("verbose:gstTabATable:gstTabATable_rowreq_ctx_init","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:210:o: |-> Perform extra gstTabATable rowreq initialization. (eg DEFVALS)
     */

    return MFD_SUCCESS;
} /* gstTabATable_rowreq_ctx_init */

/**
 * extra context cleanup
 *
 */
void gstTabATable_rowreq_ctx_cleanup(gstTabATable_rowreq_ctx *rowreq_ctx)
{
    DEBUGMSGTL(("verbose:gstTabATable:gstTabATable_rowreq_ctx_cleanup","called\n"));

    netsnmp_assert(NULL != rowreq_ctx);
    
    /*
     * TODO:211:o: |-> Perform extra gstTabATable rowreq cleanup.
     */
} /* gstTabATable_rowreq_ctx_cleanup */

/**
 * pre-request callback
 *
 *
 * @retval MFD_SUCCESS              : success.
 * @retval MFD_ERROR                : other error
 */
int
gstTabATable_pre_request(gstTabATable_registration * user_context)
{
    DEBUGMSGTL(("verbose:gstTabATable:gstTabATable_pre_request","called\n"));

    /*
     * TODO:510:o: Perform gstTabATable pre-request actions.
     */

    return MFD_SUCCESS;
} /* gstTabATable_pre_request */

/**
 * post-request callback
 *
 * Note:
 *   New rows have been inserted into the container, and
 *   deleted rows have been removed from the container and
 *   released.
 *
 * @param user_context
 * @param rc : MFD_SUCCESS if all requests succeeded
 *
 * @retval MFD_SUCCESS : success.
 * @retval MFD_ERROR   : other error (ignored)
 */
int
gstTabATable_post_request(gstTabATable_registration * user_context, int rc)
{
    DEBUGMSGTL(("verbose:gstTabATable:gstTabATable_post_request","called\n"));

    /*
     * TODO:511:o: Perform gstTabATable post-request actions.
     */

    return MFD_SUCCESS;
} /* gstTabATable_post_request */


/** @{ */
